@page
@model Proiect.Pages.Athletes.IndexModel

@{
    ViewData["Title"] = "About";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Athlete[0].Title)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Athlete[0].Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Athlete[0].Category)
            </th>
            <td>
                @Html.DisplayNameFor(model => model.Athlete[0].NextFight)
            </td>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Athlete) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Title)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Category)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.NextFight)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.ID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.ID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.ID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
